/**
 * PostCSS configuration for Next.js + Tailwind v4.
 * The @tailwindcss/postcss plugin handles all @tailwind / @import "tailwindcss"
 * directives, plus the @theme {} block in app/globals.css.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
